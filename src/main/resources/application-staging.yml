server:
  port: 8000

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: true
    fetch-registry: true

spring:
  application:
    name: tweetle-tweet-service
#  security:
#    oauth2:
#      resource server:
#        jwt:
#          jwk-set-uri:
#          issuer-uri:
#          audiences:

  data:
    mongodb:
      uri: mongodb://localhost:27017/tweetservice
      database: tweetservice
      username: admin
      password: password
